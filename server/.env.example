# Example environment for the backend
# Put real values into server/.env (do NOT commit your real API keys)
OPENAI_API_KEY=
PROVIDER=openai
MODEL=gpt-4o
PORT=3000

# If you want the backend to simulate cheaper behaviour (send shorter replies), set SIMULATE_CHUNKING=true
SIMULATE_CHUNKING=true
